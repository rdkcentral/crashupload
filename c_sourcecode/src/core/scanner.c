/**
 * @file scanner.c
 * SKELETON: Implementation needed
 */
#include "scanner.h"
#include "../../common/errors.h"
#include "../../common/constants.h"

int scanner_find_dumps(const config_t *config, dump_file_t **dumps, int *count) {
    /* TODO: Scan dump paths, find .dmp and .core files, sort by mtime */
    return ERR_NOT_IMPLEMENTED;
}
